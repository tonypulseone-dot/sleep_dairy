{$APP_DOMAIN} {
	encode zstd gzip
	reverse_proxy app:3000
}

# Другие сервисы на этом же сервере: по файлу на сайт в /opt/caddy-sites
# (в контейнере — /etc/caddy/sites). Папка вне репозитория, поэтому
# обновление дневника её не трогает. Пустая папка — не ошибка.
import /etc/caddy/sites/*.caddy
